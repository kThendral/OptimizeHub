# TODO: Centralize API URL in Frontend

## Tasks
- [x] Update `frontend/src/api/index.js` to export `API_BASE` and add `executeCustomAlgorithm` function
- [x] Update `frontend/src/components/CustomFitnessUpload.jsx` to import and use the new function instead of hardcoded URL
